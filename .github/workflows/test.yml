name: dstll-test-caller

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - id: foo
        uses: dhth/dstll-action-test@v0.1.1
        with:
          pattern: '**.go'
